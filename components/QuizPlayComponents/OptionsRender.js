import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const OptionsRender = ({options}) => {
  return (
    <View>
      <Text>OptionsRender</Text>
    </View>
  );
};

export default OptionsRender;

const styles = StyleSheet.create({});
